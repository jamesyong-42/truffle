//! `truffle ws` -- WebSocket REPL.

/// Open a WebSocket connection to a target.
pub async fn run(_target: &str, _json: bool, _binary: bool) -> Result<(), String> {
    eprintln!("truffle ws: not yet implemented (Phase 7)");
    std::process::exit(1);
}
