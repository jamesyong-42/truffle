//! `truffle chat` -- live chat with another node.

/// Start a chat session with a node.
pub async fn run(_node: &str) -> Result<(), String> {
    eprintln!("truffle chat: not yet implemented (Phase 8)");
    std::process::exit(1);
}
