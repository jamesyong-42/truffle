//! `truffle doctor` -- run diagnostics.

/// Run diagnostics on the truffle installation.
pub async fn run() -> Result<(), String> {
    eprintln!("truffle doctor: not yet implemented (Phase 10)");
    std::process::exit(1);
}
