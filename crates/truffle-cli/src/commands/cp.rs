//! `truffle cp` -- copy files between nodes.

/// Copy a file between nodes.
pub async fn run(_source: &str, _dest: &str) -> Result<(), String> {
    eprintln!("truffle cp: not yet implemented (Phase 9)");
    std::process::exit(1);
}
